use std::str::FromStr;
use crate::terms::Term::{Var, Combinator};
use crate::terms::Term;

grammar;

pub Term: Term = {
    <v:Var> => Variable(v),
    <c:Comb> => Combinator(c),
    <
}

Var: char = {
    <a:r"[a-z]"> => char::from_str(a).unwrap()
};

Comb: char = {
    <a:r"[A-Z]"> => char::from_str(a).unwrap()
};
